server:
  port: 9002

eureka:  client:    register-with-eureka: true
    service-url:       defaultZone : http://localhost:7001/eureka/ #服务中心的地址
spring:  application:    name: service-feign       